# How to use

1. Chrome → Extensions → Developer mode → Load unpacked → choose the `ls-copy` folder.
2. Open the source origin tab → extension → Read localStorage → copy or download.
3. Open the target origin tab → paste JSON (or file) → Apply.